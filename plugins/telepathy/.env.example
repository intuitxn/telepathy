# Copy to .env and fill in. Never commit .env.
BUZZ_PRIVATE_KEY=                # hex or nsec — required for the plugin to post
BUZZ_RELAY_URL=https://intuitxn.communities.buzz.xyz
# BUZZ_AUTH_TAG=                 # optional owner attestation JSON
# BUZZ_BIN=buzz                  # path to the buzz CLI if not on PATH
