# dependencies
node_modules/

# build output
dist/

# archive of reduced layers (keep local, don't track)
.archive/

# secrets
.env
.env.*
!.env.example

# macOS
.DS_Store

# private runtime state and drafts
.local

# Python runtime caches
__pycache__/
*.pyc

# generated per-program runners (absolute paths; regenerate via gen-runner)
runtime/programs/runners/

# autonomous loop transcripts (transient; record lives in agit notes)
ops/runs/
