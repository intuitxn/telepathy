```javascript
function batchMaximum(batches) {
  let max = 0;
  for (let i = 0; i < batches.length; i++) {
    const batch = batches[i];
    for (let j = 0; j < batch.length; j++) {
      if (batch[j] > max) {
        max = batch[j];
      }
    }
  }
  return max;
}
```