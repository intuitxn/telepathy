# Program: Telepathy

- **slug:** `telepathy`
- **repository:** `github.com/intuitxn/telepathy` (local checkout `~/Desktop/Attri/telepathy`)
- **channel:** `telepathy` (stream) — the program's home on the Buzz relay
- **owner:** Shubham
- **reviewers:** Om, Kush
- **roles:** the planned `@telepathy`, `@prime`, `@build`, `@steward`, `@research`, and `@relationships` interfaces; this catalog does not deploy DSH agents
- **runtime:** local DSH algorithm source candidate; the installed legacy Desk/Meta service awaits deliberate cutover
- **workflow:** exact jj review gate and independent task acceptance; the old JTBD YAML is retired
- **chat:** Buzz threads remain human context; earlier bot and Pollen/Fizz/Honey role pairings were proposals, not authenticated DSH intake
- **what it owns:** the human context layer, the Job contract, the meta-agent registry, the site alpha

Buzz threads and NIP-34 issues provide human context. They do not enter DSH
automatically or authorize a task. A host submits a reviewed local profile to
the [DSH ingress](../runtime/dsh/README.md); the independent evaluator decides
whether its result meets the frozen acceptance. The old relay intake and
historical sessions remain accessible until migration is reviewed.
